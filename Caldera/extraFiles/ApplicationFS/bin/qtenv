#!/bin/sh

# Set Qt variables for application

# Where to find the Qt shared objects.
export LD_LIBRARY_PATH=/usr/lib/

# Where to find fonts.
export QT_QWS_FONTDIR=/usr/lib/fonts

# The keyboard driver protocol and where it's located.
# export QWS_KEYBOARD=LinuxInput:/dev/input/event0:repeat-delay=5000:repeat-rate=5000

# The touch screen ("mouse") driver protocol and where it's located.
# export QWS_MOUSE_PROTO=LinuxInput:/dev/input/event1

